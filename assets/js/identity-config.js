(() => {
  "use strict";

  // Public Supabase project configuration belongs here after the project exists.
  // Never place a service-role key, OAuth secret, or provider token in this file.
  window.SEMEAI_IDENTITY_CONFIG = Object.freeze({
    schemaVersion: "1.0",
    supabaseUrl: "",
    supabasePublishableKey: "",
    siteUrl: "https://semeai.tech",
    enabled: false,
  });
})();
